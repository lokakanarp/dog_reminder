import React from 'react';

export default function Nav (props) {
	return (
		<div className="nav">
			<h1 className="h1" onClick={props.openHome}>DOG REMINDER</h1>
			<button>
      </button>
		</div>
	)
}